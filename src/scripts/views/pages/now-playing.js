const NowPlaying = {
  async render() {
    return `
            <h2>Now Playing Page</h2>
        `;
  },

  async afterRender() {
    // Fungsi akan dipanggil setelah render()
  },
};

export default NowPlaying;
